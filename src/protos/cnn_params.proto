syntax = "proto3";
package CnnProto;

message Weight {
  repeated double w = 1 [packed=true];
}

message Bias {
  repeated double b = 1 [packed=true];
}

message Params {
  repeated Weight weights = 1;
  repeated Bias biases = 2;
}
